# ⚠️ ACTION REQUIRED: Add your valid PostgreSQL URL in your hosting provider's Environment Variables (e.g. Vercel)
DATABASE_URL="postgresql://user:password@host/database"

# Next Auth Secret (Generate a strong random string)
AUTH_SECRET="your-random-secret"

# OpenRouter AI
OPENROUTER_API_KEY="your-openrouter-key"
