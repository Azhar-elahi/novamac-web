// NOTE: Deprecated/unused duplicate of ServicePageClient with an old data
// shape (overview/deliverables/idealFor). The live route
// (services/[slug]/page.tsx) renders ServicePageClient instead. This file
// is kept only as a placeholder re-export so nothing breaks if referenced.
export { default } from "@/components/services/ServicePageClient";
